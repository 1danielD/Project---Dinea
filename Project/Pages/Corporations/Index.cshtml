@page
@model Project.Pages.Corporations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Corporation[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Corporation[0].Department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Corporation[0].Task)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Corporation[0].CorporationID)
            </th>

            <th>@Html.DisplayNameFor(model => model.Corporation[0].PublishingDate)</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Corporation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Task)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
